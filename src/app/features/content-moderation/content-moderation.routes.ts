import { Routes } from '@angular/router';
import { ContentModerationComponent } from './content-moderation.component';

export const contentModerationRoutes: Routes = [
  {
    path: '',
    component: ContentModerationComponent
  }
];